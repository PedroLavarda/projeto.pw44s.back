spring:
  application:
    name: server
  datasource:
    url: jdbc:postgresql://localhost:5432/gamestart
    username: postgres
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true